- name: install hunk (terminal diff viewer for agent-authored changesets)
  shell: npm install -g --prefix {{ home }}/.local hunkdiff
  args:
    creates: "{{ home }}/.local/bin/hunk"
  tags:
    - install
    - hunk
